import { useState } from "react"

const SettingsUser = () => {

    // const [list, setList] = useState([])
    // const [listItems, setListItems] = useState()

    // const testFetch = () => {
    //     const fetchList = async () => {
    //       try {
    //         const response = await fetch('http://localhost:3001/lists/1');
    //         if (!response.ok) {
    //           throw new Error('Network response was not ok');
    //         }
    //         const [listData] = await response.json(); // Destructure the first object from the array directly
    //         console.log(listData);
    //         console.log(listData.items);
    //         setList(listData || {}); // Set the list state directly to the object
    //         setListItems(listData.items || []); // Set the listItems state with the items array from the object
    //       } catch (error) {
    //         console.error(error);
    //       }
    //     };
    //     fetchList();
    //   };
      

    // const testPost = () => {
            
    // }

    // const showResult = () => {
    //     console.log(list)
    //     console.log(listItems)
    // }

    return (
        <>
        {/* <button onClick={testFetch}>get list</button>
        <button onClick={testPost}>add to list</button>
        <button onClick={showResult}>console log list</button> */}
        </>
    )
}

export default SettingsUser